{% extends "layout/base.twig" %}

{% block content %}
    <article class="page404">
        <div class="page404-wrapper">
            <div class="page404-number">404</div>
            <div class="page404-title">{{ __('La page demandée n\'a pas été trouvée','bim') }}</div>

            <a href="{{ site.link }}" class="page404-contact button-secondary">
                <span class="button-text">{{ __('Retour à l\'accueil') }}</span>
            </a>
        </div>
    </article>
{% endblock %}
